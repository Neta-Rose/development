# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
generate-sqlite/__marimo__
.swiftpm/
migrate_working_dir/
.agents
build

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.vercel
.env*
# ...but the backend's documented template is not a secret.
!server/.env.example

# server: Go build output. Nothing generated is committed.
/server/plated
/server/plated.exe

# infra: provider downloads, plan files, and state. The app stack's state lives in S3;
# the bootstrap stack's is local and disposable — but it still names IAM roles and the
# state bucket, so it is not committed either.
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
# Real values belong in GitHub secrets or a local file, never in git. The `.example` is
# the documented template, same arrangement as server/.env.example.
*.tfvars
*.tfvars.json
!*.tfvars.example

# generate-sqlite: pipeline data. The DuckDB is the source of truth but is a
# local artifact — the only output that ships is database/foods.sqlite, which
# Stage 7 writes there directly and which *is* tracked.
generate-sqlite/data/*.duckdb*
generate-sqlite/data/backup/
generate-sqlite/data/log.sqlite
generate-sqlite/data/raw/*
!generate-sqlite/data/raw/.gitkeep
__pycache__/
.venv/

# upload-staged-patch skill: locally generated patches of the index, plus the
# metadata for the last filebin upload. Transient artifacts, never committed.
.kiro/patches/
